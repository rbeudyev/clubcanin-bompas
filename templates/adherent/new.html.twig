{% extends 'base.html.twig' %}

{% block title %}Nouvel Adhérent - Club Canin de Bompas{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="text-center mb-5">
                <h1 class="display-4 fw-bold text-white mb-3">
                    <i class="bi bi-person-plus-fill me-3"></i>Nouvel Adhérent
                </h1>
                <p class="lead text-light">Ajoutez un nouveau membre à notre club canin</p>
            </div>

            {{ include('adherent/_form.html.twig') }}

            <div class="text-center mt-4">
                <a href="{{ path('app_adherent_index') }}" class="btn btn-outline-light btn-lg">
                    <i class="bi bi-arrow-left me-2"></i>Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
